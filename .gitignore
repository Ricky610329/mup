node_modules/
dist/
.DS_Store
*.local
.env
.claude/
CLAUDE.md

# MUP workspace state
.mup/

# MUP archive — keep examples, ignore build artifacts
archive/mup-agent/node_modules/
archive/mup-agent/dist/

# npm auth (use ~/.npmrc for local, NPM_TOKEN env for CI)
.npmrc

# MCP Registry tokens and publisher binary
.mcpregistry_*
mcp-publisher.exe

# Local workspace tools (not part of open-source repo)
tools/
