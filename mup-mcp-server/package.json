{
  "name": "mup-mcp-server",
  "version": "0.1.0",
  "description": "MCP server bridge for MUP (Model UI Protocol) — use MUP skills in Claude Desktop, Cursor, and any MCP client",
  "type": "module",
  "bin": {
    "mup-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "typescript": "^5.7.0"
  }
}
